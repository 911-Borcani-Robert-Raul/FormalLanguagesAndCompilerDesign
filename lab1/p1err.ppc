fun maximum: a b
    if a > b:           // ERROR, should be if [...] then
        result = a
    el
        result = b

fun minimum : a b
    if a < b then
        return a        // ERROR, this is not how you return
    el
        return b


write: maximum:3 4::
write: minimum:3 4::
